---
type: Task
id: python_dotenv_autoload_20260826:2.1
title: "Implement package-level auto-loading hook with test guards in src/goe/__init__.py"
state: closed
priority: P1
verification_strategy: unit_tests
depends_on:
  - python_dotenv_autoload_20260826:1.2
files:
  - src/goe/__init__.py
tests:
  - tests/unit/config/test_config_file.py
  - tests/unit/config/test_orchestration_config.py
plan_revision: 1
plan_commit: null
state_revision: 7
claimed_by: null
claimed_at: null
blocked_reason: null
unblock_condition: null
next_step: null
last_operation: 20260828T021200Z-flow-executor-close-2-1-00
operation_targets:
  - "2.1"
last_verified_at: "2026-08-28T02:12:00Z"
last_verified_commit: db6fe320b0a8ba935fa6ee42f8f30b7ecdb6a95e
verification_evidence:
  - command: "uv run pytest tests/unit/config/test_config_file.py -v"
    result: "20 passed in 0.11s"
  - command: "uv run ruff check src/goe/__init__.py tests/unit/config/test_config_file.py"
    result: "All checks passed!"
  - command: "uv run mypy src/goe/__init__.py"
    result: "Success: no issues found in 1 source file"
created_at: "2026-08-26T15:48:00Z"
updated_at: "2026-08-28T02:12:00Z"
commit: db6fe320b0a8ba935fa6ee42f8f30b7ecdb6a95e
---

# Task 2.1: Implement package-level auto-loading hook with test guards in src/goe/__init__.py

## Objective
Add an automatic environment loading hook to [`src/goe/__init__.py`](file:///usr/local/google/home/codyfincher/code/gluent/next-goe/src/goe/__init__.py) that initializes `offload.env` when `goe` is imported, while protecting isolated unit test runs (`PYTEST_CURRENT_TEST`) and respecting explicit opt-out directives (`GOE_NO_AUTOLOAD_ENV=1`).

## Context
When developers or automated workflows import `goe` submodules or run commands, having environment variables populated automatically upon package entry guarantees that module-level constants and default factories receive valid configuration without requiring explicit bootstrap boilerplate.

## Steps
1. In [`src/goe/__init__.py`](file:///usr/local/google/home/codyfincher/code/gluent/next-goe/src/goe/__init__.py), define `_autoload_environment() -> None`:
   - Check if `os.environ.get("GOE_NO_AUTOLOAD_ENV")` is set to `"1"` or `"true"`. If so, skip auto-loading.
   - Check if `"PYTEST_CURRENT_TEST"` in `os.environ`. If so, skip auto-loading to preserve mock test environments.
   - Import `load_env` from `goe.config.config_file` and invoke `load_env()`.
2. Call `_autoload_environment()` during package initialization.
3. Ensure no circular imports occur when importing `goe.config.config_file`.
4. Ensure no inline comments are present in the Python code, with any design rationale placed in the docstring.

## Verification
- Run `uv run pytest tests/unit/` to verify all unit tests pass without unintended environment pollution.
- Author a dedicated test verifying that `_autoload_environment()` respects `GOE_NO_AUTOLOAD_ENV` and `PYTEST_CURRENT_TEST`.
- Run `uv run ruff check src/goe/__init__.py` and `uv run mypy src/goe/__init__.py`.

## Acceptance Criteria
- [x] Importing `goe` automatically loads `offload.env` if present in discovery path.
- [x] Auto-loading is safely bypassed when running under `pytest` (`PYTEST_CURRENT_TEST`).
- [x] Auto-loading is safely bypassed when `GOE_NO_AUTOLOAD_ENV=1` is set.
- [x] No circular import errors or performance regressions on package import.
