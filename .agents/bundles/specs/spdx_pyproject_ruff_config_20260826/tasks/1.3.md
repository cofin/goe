---
type: Task
id: spdx_pyproject_ruff_config_20260826:1.3
title: Extended edge case testing and CLI integration tests for migration tooling
state: closed
priority: P2
verification_strategy: behavior_tdd
depends_on:
  - spdx_pyproject_ruff_config_20260826:1.2
files:
  - tests/unit/test_migrate_spdx_headers.py
tests:
  - tests/unit/test_migrate_spdx_headers.py
plan_revision: 2
plan_commit: null
state_revision: 2
claimed_by: executor
claimed_at: "2026-08-26T21:26:00Z"
blocked_reason: null
unblock_condition: null
next_step: null
last_operation: close
operation_targets:
  - tests/unit/test_migrate_spdx_headers.py
last_verified_at: "2026-08-26T21:26:30Z"
last_verified_commit: 68f0342
verification_evidence: "uv run pytest tests/unit/test_migrate_spdx_headers.py passed 16/16 tests with full edge case and CLI flag coverage"
created_at: "2026-08-26T15:20:00Z"
updated_at: "2026-08-26T21:26:30Z"
commit: 68f0342
---

# Task 1.3: Extended edge case testing and CLI integration tests for migration tooling

## Objective
Expand [tests/unit/test_migrate_spdx_headers.py](file:///usr/local/google/home/codyfincher/code/gluent/next-goe/tests/unit/test_migrate_spdx_headers.py) with comprehensive edge cases including multi-line year ranges (`2016-2024`), CRLF line endings, XML/Jinja templates, CSS/Scala blocks, CLI `--dry-run` output formatting, and idempotency.

## Context
Before running the tool across hundreds of production source files in Chapters 2 & 3, all edge cases and CLI flags must be verified with automated tests.

## Steps
1. Add edge case tests to `tests/unit/test_migrate_spdx_headers.py`:
   - `test_migrate_html_xml_jinja_header()`: `<!-- ... -->` comment formatting.
   - `test_migrate_c_style_header()`: `/* ... */` comment formatting for Scala, JS, CSS.
   - `test_preserve_copyright_year_ranges()`: Tests `2016`, `2024`, and `2016-2024`.
   - `test_crlf_line_endings()`: Ensures Windows `\r\n` files are parsed and transformed correctly.
   - `test_idempotency()`: Running migration on an already-converted file results in no changes.
   - `test_unrecognized_file()`: Files without Apache headers remain untouched.
   - `test_cli_dry_run_flag()`: Validates CLI `--dry-run` and `--check` return codes.
2. Run pytest suite:
   ```bash
   uv run pytest tests/unit/test_migrate_spdx_headers.py -v
   ```

## Verification
- `uv run pytest tests/unit/test_migrate_spdx_headers.py` passes 100% with all edge cases covered.

## Acceptance Criteria
- [x] Extended test cases cover all repository file types and edge cases.
- [x] CLI flags (`--dry-run`, `--check`) tested and passing.

## Notes & Discoveries
- Added tests for CRLF line endings, year ranges (e.g. `2016-2024`), HTML/Jinja/CSS/Scala comments, idempotency, and `--dry-run` flag.
- All 16 unit tests in `tests/unit/test_migrate_spdx_headers.py` passing 100%.
